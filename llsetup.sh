#!/bin/bash
# script to setup a new linux lab node project
# the first line denotes this as a bash script file so the OS knows how to handle it
# sets up node and creates your package.json and other files
npm init
# add dependancy and vertion to package.json
# download the dependancy and put it into node_modules
npm i --save express
#npm i --save fs
# npmi install
# this is all you need for an existing project
#npm i
